# GeonicDB connection. Copy to .env.local and adjust if needed.
# No trailing slash — see normalizeBaseUrl() in src/geonic.ts.
VITE_GEONICDB_URL=https://geonicdb.geolonia.com
VITE_GEONICDB_TENANT=ohashi
